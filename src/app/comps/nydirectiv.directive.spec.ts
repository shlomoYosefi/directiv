import { NydirectivDirective } from './nydirectiv.directive';

describe('NydirectivDirective', () => {
  it('should create an instance', () => {
    const directive = new NydirectivDirective();
    expect(directive).toBeTruthy();
  });
});
