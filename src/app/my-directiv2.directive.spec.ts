import { MyDirectiv2Directive } from './my-directiv2.directive';

describe('MyDirectiv2Directive', () => {
  it('should create an instance', () => {
    const directive = new MyDirectiv2Directive();
    expect(directive).toBeTruthy();
  });
});
